var nombre="Juan Daniel"; //se utiliza la palabra var para declarar una variable se puede declarar sin inicializarla si la variable no se inicializa agrega undefine
console.log(nombre);
nombre="otro nombre";
console.log(nombre);
var nombre=25;
console.log(nombre);
let apellido; //se utiliza para declarar variables y al igual que var se puede declarar sin inicializar pero let no asigna el valor undefined
//let no permite redefinir la variable como en el caso de var
console.log(apellido);
apellido="Carrillo";
//otra opcion para definir variables es tambien para declarar variables, const no sirve para declarar variables
const materno="Mercado"; //const siempre hay que inicializar la variable, y al igual que let no permite redefinir

